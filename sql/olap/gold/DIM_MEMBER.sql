CREATE TABLE IF NOT EXISTS GOLD.DIM_MEMBER (
  MEME_KEY BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  MEME_ID STRING NOT NULL,
  MESB_ID STRING NOT NULL,
  FIRST_NAME STRING NOT NULL,
  LAST_NAME STRING NOT NULL,
  DATE_OF_BIRTH DATE NOT NULL,
  GENDER_CODE STRING,
  RELATIONSHIP_CODE STRING NOT NULL,
  EFFECTIVE_DATE DATE NOT NULL,
  END_DATE DATE,
  CURRENT_FLAG BOOLEAN NOT NULL
)
USING DELTA
COMMENT 'Gold member dimension - SCD Type 2.';
